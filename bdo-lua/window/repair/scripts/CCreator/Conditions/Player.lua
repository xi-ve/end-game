Conditions.playerHpLessThanCondition = {
    index = 5,
    text = "Player Hp less than ",
    hasValue = true,
    valueIsString = false,
    code = "Player.getHp() < "
}

Conditions.playerHpMoreThanCondition = {
    index = 6,
    text = "Player Hp more than ",
    hasValue = true,
    valueIsString = false,
    code = "Player.getHp() > "
}

Conditions.playerHpPctLessThanCondition = {
    index = 5,
    text = "Player Hp% less than ",
    hasValue = true,
    valueIsString = false,
    code = "Player.getHpPercent() < "
}

Conditions.playerHpPctMoreThanCondition = {
    index = 6,
    text = "Player Hp% more than ",
    hasValue = true,
    valueIsString = false,
    code = "Player.getHpPercent() > "
}

Conditions.playerMpLessThanCondition = {
    index = 6,
    text = "Player Mp less than ",
    hasValue = true,
    valueIsString = false,
    code = "Player.getMp() < "
}

Conditions.playerMpMoreThanCondition = {
    index = 7,
    text = "Player Mp more than ",
    hasValue = true,
    valueIsString = false,
    code = "Player.getMp() > "
}

Conditions.playerMpPctLessThanCondition = {
    index = 6,
    text = "Player Mp% less than ",
    hasValue = true,
    valueIsString = false,
    code = "Player.getMpPercent() < "
}

Conditions.playerMpPctMoreThanCondition = {
    index = 7,
    text = "Player Mp% more than ",
    hasValue = true,
    valueIsString = false,
    code = "Player.getMpPercent() > "
}

Conditions.playerWpLessThanCondition = {
    index = 8,
    text = "Player Wp less than ",
    hasValue = true,
    valueIsString = false,
    code = "Player.getWp() < "
}

Conditions.playerWpMoreThanCondition = {
    index = 9,
    text = "Player Wp more than ",
    hasValue = true,
    valueIsString = false,
    code = "Player.getWp() > "
}

Conditions.playerWpPctLessThanCondition = {
    index = 8,
    text = "Player Wp% less than ",
    hasValue = true,
    valueIsString = false,
    code = "Player.getWpPercent() < "
}

Conditions.playerWpPctMoreThanCondition = {
    index = 9,
    text = "Player Wp% more than ",
    hasValue = true,
    valueIsString = false,
    code = "Player.getWpPercent() > "
}
