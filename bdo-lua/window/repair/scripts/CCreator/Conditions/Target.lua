Conditions.targetDistanceOver = {
    index = 1,
    text = "Distance to target less than ",
    hasValue = true,
    valueIsString = false,
    code = "distToTarget < "
}

Conditions.targetDistanceLess = {
    index = 2,
    text = "Distance to target more than ",
    hasValue = true,
    valueIsString = false,
    code = "distToTarget > "
}


Conditions.targetHpLessThanCondition = {
    index = 3,
    text = "Target Hp less than ",
    hasValue = true,
    valueIsString = false,
    code = "target:getHp() < "
}

Conditions.targetHpMoreThanCondition = {
    index = 4,
    text = "Target Hp more than ",
    hasValue = true,
    valueIsString = false,
    code = "target:getHp() > "
}
